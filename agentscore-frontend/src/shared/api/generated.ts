// AUTO-GENERATED STUB - the real file is a 22k-line openapi-typescript dump of
// the live backend schema. No backend here, so every schema resolves to `any`
// via this one loose index signature instead of being hand-copied.
export interface components {
  schemas: Record<string, any>;
}
